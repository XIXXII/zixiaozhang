<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AddItemActivity">

  <androidx.appcompat.widget.Toolbar
      android:id="@+id/listHeader2"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:background="#FFFFFF"
      android:minHeight="?attr/actionBarSize"
      android:theme="?attr/actionBarTheme"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintTop_toTopOf="parent" />

  <ImageButton
      android:id="@+id/backListDetail"
      android:layout_width="64dp"
      android:layout_height="55dp"
      android:background="@android:color/transparent"
      android:onClick="backDetailPage"
      app:layout_constraintStart_toStartOf="@id/listHeader2"
      app:layout_constraintTop_toTopOf="parent"
      app:srcCompat="@drawable/back" />

  <TextView
      android:layout_width="234dp"
      android:layout_height="53dp"
      android:layout_marginStart="196dp"
      android:layout_marginTop="0dp"
      android:fontFamily="@font/baloo_da"
      android:text="Add Item"
      android:textAlignment="center"
      android:textColor="#43658B"
      android:textSize="36sp"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintTop_toTopOf="@id/listHeader2" />

  <ExpandableListView
      android:id="@+id/hierarchical_list"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:layout_marginTop="120dp"
      android:childDivider="@android:color/transparent" />

  <Button
      android:id="@+id/button"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginStart="5dp"
      android:layout_marginLeft="10dp"
      android:layout_marginTop="60dp"
      android:layout_marginEnd="10dp"
      android:background="@drawable/button_border"
      android:drawableEnd="@drawable/next"
      android:gravity="start"
      android:onClick="toSearchPage"
      android:paddingStart="20dp"
      android:paddingTop="8dp"
      android:text="Search for Item Name"
      android:textColor="#43658B"
      android:textSize="19sp" />
</RelativeLayout>